<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.siga">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Siga">

        <activity android:name=".Exam">
            android:label = "Exam"
            android:parentActivityName="com.example.android.siga.Home">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Home"></meta-data>
        </activity>

        <activity android:name=".Approve" >
            android:label = "Approve"
            android:parentActivityName="com.example.android.siga.Home">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Home"></meta-data>
        </activity>

        <activity android:name=".Register">
            android:label = "Register"
            android:parentActivityName="com.example.android.siga.Home">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Home"></meta-data>
        </activity>

        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>